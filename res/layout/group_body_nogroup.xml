
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" >

        <Button
            android:id="@+id/btn_group_join"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.0"
            android:background="@drawable/shape_mainmenu_button"
            android:drawableTop="@drawable/user_club_find"
            android:onClick="groupClick"
            android:text="Join"
            android:textAppearance="?android:attr/textAppearanceLarge" />

        <Button
            android:id="@+id/btn_group_create"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.0"
            android:background="@drawable/shape_mainmenu_button"
            android:drawableTop="@drawable/user_club_create"
            android:onClick="groupClick"
            android:text="Create"
            android:textAppearance="?android:attr/textAppearanceLarge" />

    </LinearLayout>
